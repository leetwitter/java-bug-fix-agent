org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# Pinned dependency versions (single source of truth)
langchain4jVersion=1.2.0
langchain4jOllamaVersion=1.2.0
langchain4jOpenAiVersion=1.2.0
# Community MCP-server module is versioned on its own line (1.15.0-betaN), not the
# core 1.2.0 pin. Isolated to agent-mcp, which runs NO LLM, so the ollama-tool-call
# reason for pinning core at 1.2.0 does not apply here.
langchain4jCommunityMcpServerVersion=1.15.0-beta25
javaparserVersion=3.27.0
junitVersion=5.10.3
mockitoVersion=5.12.0
assertjVersion=3.26.3
